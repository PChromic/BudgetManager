package com.pchromic.BudgetManager.enums;

public enum ReportType {
    QUARTER,
    HALFYEAR,
    YEAR,
    HOME,
    FOOD,
    CHILD_RELATED,
    DEBT,
    HEALTH,
    TRANSPORTATION,
    PERSONAL,
    PET,
    ENTERTAINMENT,
    MISCELLANEOUS,


   /* private final String description;

    ReportType(final String description) {
        this.description = description;
    }

    public static ReportType getByDescription(final String description) {
        for (ReportType r : values()
                ) {
            if (r.description.equals(description))
                return r;
        }
        return null;
    }*/
}
